unbind C-b
set -g prefix C-a

set-window-option -g mode-keys vi
bind-key -t vi-copy 'v' begin-selection
bind-key -t vi-copy 'y' copy-selection

# fix for OS X clipboard
set-option -g default-command "reattach-to-user-namespace -l bash"
